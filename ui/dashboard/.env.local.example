# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:4000

# Note: API key for manual triggers is entered directly in the Settings page
# This keeps the key secure and not exposed in browser environment variables
